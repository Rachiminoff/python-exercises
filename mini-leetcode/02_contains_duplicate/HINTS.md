# 💡 Hints — Contains Duplicate

*Tags: array, set*

Try the problem for real first. These are ordered from vague to specific — stop reading as soon as you
see a way forward.

## Hints

1. A set only ever keeps unique values — what happens when you convert nums to a set?
2. Compare len(set(nums)) to len(nums).

## Relevant functions & syntax

`set()`, `len()`

[← back to mini-leetcode](README.md)
