# 💡 Hints — Valid Palindrome

*Tags: string, two pointers*

Try the problem for real first. These are ordered from vague to specific — stop reading as soon as you
see a way forward.

## Hints

1. str.isalnum() tells you whether a character is a letter or digit.
2. Build a cleaned version of the string first — lowercase, letters/digits only — then compare it to its reverse.
3. Slicing a string with [::-1] reverses it.

## Relevant functions & syntax

`str.isalnum()`, `str.lower()`, `slicing [::-1]`

[← back to mini-leetcode](README.md)
