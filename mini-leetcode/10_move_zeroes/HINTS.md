# 💡 Hints — Move Zeroes

*Tags: array, two pointers*

Try the problem for real first. These are ordered from vague to specific — stop reading as soon as you
see a way forward.

## Hints

1. Two pointers: one scans through the list, the other marks where the next non-zero value should land.
2. When the scanning pointer finds a non-zero, swap it into the landing position and advance both pointers.

## Relevant functions & syntax

`two-pointer pattern`, `tuple swap a, b = b, a`

[← back to mini-leetcode](README.md)
