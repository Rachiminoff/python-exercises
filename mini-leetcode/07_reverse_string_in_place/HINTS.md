# 💡 Hints — Reverse String In-Place

*Tags: string, two pointers*

Try the problem for real first. These are ordered from vague to specific — stop reading as soon as you
see a way forward.

## Hints

1. Two pointers — one at each end of the list — swapping and moving inward, works well here.
2. Or: does Python's list already have a method that reverses in place?
3. 'In place' means don't create and return a new list — mutate the one you were given.

## Relevant functions & syntax

`list.reverse()`, `two-pointer swap pattern`

[← back to mini-leetcode](README.md)
