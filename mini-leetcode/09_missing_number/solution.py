"""
Problem #9: Missing Number
Difficulty: Easy   Tags: array, math

Given a list containing n distinct numbers from the range 0 to n (inclusive), find the one number in that range that is missing from the list.

Examples:
    Input:  nums = [3, 0, 1]
    Output: 2  (range is 0..3, 2 is missing)
    Input:  nums = [0, 1]
    Output: 2

Stuck? See HINTS.md in this folder.

Rules:
    - No AI — no Copilot / ChatGPT / autocomplete you didn't type yourself.
    - Google is fine for syntax, docs, and built-in functions.
"""

def missing_number(nums: list[int]) -> int:
    # TODO: write your solution below
    pass


if __name__ == "__main__":
    # Run this file directly to check your solution against the examples above.
    assert missing_number([3, 0, 1]) == 2, "Test 1 failed"
    assert missing_number([0, 1]) == 2, "Test 2 failed"
    print("All sample tests passed!")
