"""
Problem #1: Two Sum
Difficulty: Easy   Tags: array, hashmap

Given a list of integers nums and an integer target, return the indices of the two numbers that add up to target. Assume exactly one valid answer exists, and you may not use the same element twice.

Examples:
    Input:  nums = [2, 7, 11, 15], target = 9
    Output: [0, 1]  (nums[0] + nums[1] == 9)
    Input:  nums = [3, 2, 4], target = 6
    Output: [1, 2]

Rules:
    - No AI — no Copilot / ChatGPT / autocomplete you didn't type yourself.
    - Google is fine for syntax, docs, and built-in functions.
"""

def two_sum(nums: list[int], target: int) -> list[int]:
    # TODO: write your solution below
    pass


if __name__ == "__main__":
    # Run this file directly to check your solution against the examples above.
    assert two_sum([2, 7, 11, 15], 9) == [0, 1], "Test 1 failed"
    assert two_sum([3, 2, 4], 6) == [1, 2], "Test 2 failed"
    print("All sample tests passed!")
