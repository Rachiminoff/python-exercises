"""
Mini Project #11: Escape the Room 🚪

Targets: if/elif/else chains (Exercise #7).

Build one program that meets all of these requirements:
    - Present a short choose-your-own-adventure: the player is locked in a
      room with at least 3 distinct choices at the first decision point
      (e.g. "search the desk", "check the window", "bang on the door").
    - Use an if/elif/else chain (not a dict lookup) to branch on the choice
      and print a different story beat for each one.
    - At least one path should lead to a second decision point with its own
      if/elif/else chain (e.g. finding a key leads to "use it" or "keep
      looking").
    - Exactly one full path through the story should end in "You escaped!" —
      all others should end in some flavor of "You're still stuck.".

Stuck? See HINTS.md in this folder.

Rules:
    - No AI — no Copilot / ChatGPT / autocomplete you didn't type yourself.
    - Google is fine for syntax, docs, and built-in functions.
"""

# TODO: write your solution below


def main():
    pass


if __name__ == "__main__":
    main()
