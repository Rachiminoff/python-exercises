"""
Mini Project #13: NPC Reaction Generator 🎭

Targets: conditional (ternary) expressions (Exercise #12).

Build one program that meets all of these requirements:
    - Ask for a handful of inputs describing an encounter with an NPC:
      has_met_before, gift_given, and the NPC's mood ("friendly", "neutral",
      or "hostile").
    - Using ONLY ternary expressions (no multi-line if/elif/else blocks
      anywhere in main()), compute:
        - a greeting: different for first meeting vs. returning visitor
        - a reaction to the gift: different if a gift was given vs. not
        - a mood line: a *nested* ternary picking between three lines, one
          per mood
    - Print all three lines together as one combined "dialogue" block.
    - Let the user generate another NPC's reaction in a loop until they type
      'quit'.

Stuck? See HINTS.md in this folder.

Rules:
    - No AI — no Copilot / ChatGPT / autocomplete you didn't type yourself.
    - Google is fine for syntax, docs, and built-in functions.
"""

# TODO: write your solution below


def main():
    pass


if __name__ == "__main__":
    main()
