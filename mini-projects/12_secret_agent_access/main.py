"""
Mini Project #12: Secret Agent Access Control 🕵️

Targets: logical operators — and / or / not (Exercise #11).

Build one program that meets all of these requirements:
    - Define at least 3 rooms in a facility, each with its own access rule
      built from booleans like has_keycard, has_clearance_level_2,
      is_escorted_by_senior_agent, and is_alarm_active.
    - At least one room's rule must combine and/or/not in the same
      expression (e.g. "keycard AND (clearance OR escorted) AND NOT alarm").
    - Ask the agent (the user) yes/no questions to gather their booleans,
      then check them against every room's rule.
    - Print "ACCESS GRANTED" or "ACCESS DENIED" for each room, and for a
      denied room, print which specific condition failed.

Stuck? See HINTS.md in this folder.

Rules:
    - No AI — no Copilot / ChatGPT / autocomplete you didn't type yourself.
    - Google is fine for syntax, docs, and built-in functions.
"""

# TODO: write your solution below


def main():
    pass


if __name__ == "__main__":
    main()
